name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    name: Build for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        include:
          - os: ubuntu-latest
            asset_name: xzssh-linux
          - os: windows-latest
            asset_name: xzssh-windows.exe
          - os: macos-latest
            asset_name: xzssh-macos

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      # The onefile binaries fall back to xzssh._FALLBACK_VERSION when
      # package metadata isn't bundled — refuse to ship a binary whose
      # fallback (or pyproject version) doesn't match the tag.
      - name: Verify version consistency
        shell: bash
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            python scripts/check_version.py "${GITHUB_REF_NAME}"
          else
            python scripts/check_version.py
          fi

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install . nuitka

      - name: Build with Nuitka
        uses: Nuitka/Nuitka-Action@main
        with:
          nuitka-version: main
          script-name: xzssh/cli/main.py
          # `onefile: true` was removed upstream — newer Nuitka rejects mixing
          # the deprecated --onefile flag with the new --mode=. Use mode:
          # onefile instead, which produces the same single-binary output.
          mode: onefile
          # Output file and artifact folder share one name so the release
          # step's glob (artifacts/<asset>/<asset>) actually matches the
          # binary inside the downloaded folder.
          output-file: ${{ matrix.asset_name }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: build/${{ matrix.asset_name }}

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    # Required so GITHUB_TOKEN can create the release. Without this the
    # token defaults to read-only and the API returns 403.
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Extract release notes for this tag
        run: |
          # Populate the Release body with just this version's CHANGELOG
          # section. Fall back to a pointer if extraction fails for any reason.
          if python3 scripts/extract_changelog.py "${GITHUB_REF_NAME}" > RELEASE_NOTES.md; then
            echo "Extracted notes for ${GITHUB_REF_NAME}:"
            cat RELEASE_NOTES.md
          else
            echo "See [CHANGELOG.md](CHANGELOG.md) for release notes." > RELEASE_NOTES.md
          fi

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          body_path: RELEASE_NOTES.md
          files: |
            artifacts/xzssh-linux/xzssh-linux
            artifacts/xzssh-windows.exe/xzssh-windows.exe
            artifacts/xzssh-macos/xzssh-macos
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
